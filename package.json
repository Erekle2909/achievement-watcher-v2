{
  "name": "achievement-watcher-v2",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --build",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "npx rimraf packages/*/dist apps/*/dist plugins/*/dist"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
